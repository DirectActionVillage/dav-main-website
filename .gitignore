# Hidden Files
.*

# VSCode
# Ignore the whole directory. Git does not descend into an ignored directory,
# so nothing inside can be re-included without un-ignoring the directory first.
.vscode/
*.code-workspace

# Built Visual Studio Code Extensions
*.vsix

# Node Modules
node_modules
node_modules/*
